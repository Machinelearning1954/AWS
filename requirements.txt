pyspark==3.5.0
xgboost>=1.6.0
scikit-learn>=1.3.0 # scikit-learn is a dependency of xgboost.spark, ensure a compatible version
numpy # usually a transitive dependency, but good to list if specific operations rely on it.
